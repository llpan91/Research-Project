1. 什么是 Diffusion Policy？详细介绍 Diffusion Policy 这个概念？

Diffusion Policy（扩散策略）本质上是把“动作生成”建模成一个条件扩散生成过程。它在机器人模仿学习里最常见，不一定是传统意义上的强化学习（RL）。

核心思路可以分成三步：

- 正向扩散：先把真实动作序列逐步加噪，直到接近高斯噪声。
- 反向去噪：训练一个网络，在给定观测条件（图像、状态、历史动作等）下，逐步把噪声还原成可执行动作。
- 推理采样：部署时从噪声开始迭代去噪，采样出未来一段动作轨迹（action chunk）。

为什么它适合机器人操作：

- 能建模多模态动作分布：同一观测下，可能有多种都合理的动作（如顺时针/逆时针绕开障碍）。扩散模型不会像 MSE 回归那样“平均化”成无效中间动作。
- 对高维连续动作更稳定：生成整段轨迹而不是一步点估计，时序更平滑。
- 容易结合视觉和多模态传感器：可条件化在 RGB、深度、力觉、本体状态等输入上。

在你这类 UMI/UMI-FT 语境里，Diffusion Policy 通常扮演“高层视觉运动策略”：输入多模态观测，输出未来的位姿参考、抓取相关量或其他控制目标，再交给低层控制器高频跟踪。



2. 为什么输出的是 21 维动作向量（包含参考位姿 9D），而不是 6DoF 位姿？

先说结论：6DoF 只是“任务空间位姿自由度”的物理语义，不等于网络输出必须是 6 个数。你这里的 21 维是“控制架构需要的完整指令集”，不是单一末端位姿。

根据你文档中的定义，21 维由以下部分组成：

- 参考位姿（9D）
- 虚拟目标位姿（9D）
- 刚度（1D）
- 夹爪宽度 + 抓取力（2D）

总计：9 + 9 + 1 + 2 = 21。

为什么位姿是 9D 而不是“6 个数”：

- 6DoF 描述的是 3 个平移 + 3 个转动自由度。
- 但旋转在网络里常不用欧拉角 3D 直接回归，因为欧拉角有奇异性、角度跳变、优化不稳定等问题。
- 因此常用更稳定的旋转参数化（例如 6D rotation representation）。这样位姿就变成：
	- 平移 3D
	- 旋转表示 6D
	- 合计 9D

所以“9D 位姿”并没有增加物理自由度，只是换了更适合学习与优化的数学表示。

为什么还要输出“虚拟目标位姿 + 刚度 + 力/宽度”：

- 该系统不是纯位置控制，而是“策略层 + 柔顺/力控制层”的分层控制。
- 策略不仅要给几何轨迹，还要给交互参数（刚度、抓取力等），让低层控制器在接触任务中实现稳定、柔顺、可控的力学行为。
- 这也是输出维度超过单纯 6DoF 的根本原因。